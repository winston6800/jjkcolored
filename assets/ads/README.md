# Ad assets

Drop the real creative here as:

```
assets/ads/dead-by-default.png
```

The reader margin ad units in `index.html` already point at this exact path
and link to `https://deadbydefault.app`. As soon as the file exists here, it
replaces the CSS-drawn fallback card automatically — no code changes needed.
